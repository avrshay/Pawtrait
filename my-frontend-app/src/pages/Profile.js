// PUT /users/profile/:id — the logged-in user editing their own details.
export default function Profile() {
  return (
    <section>
      <h1>Profile</h1>
      <p>Backend: <code>GET /users/:id</code>, <code>PUT /users/profile/:id</code></p>
    </section>
  );
}
