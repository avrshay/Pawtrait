// GET /gallery — public storefront listing of all products.
export default function Gallery() {
  return (
    <section>
      <h1>Gallery</h1>
      <p>Browse products. Backend: <code>GET /gallery</code></p>
    </section>
  );
}
