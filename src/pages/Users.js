// GET /users — admin/manager list of all users.
export default function Users() {
  return (
    <section>
      <h1>Users (Admin)</h1>
      <p>Backend: <code>GET /users</code>, <code>POST /users</code>, <code>DELETE /users/:id</code></p>
    </section>
  );
}
